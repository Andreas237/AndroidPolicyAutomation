query NotificationsQuery($customerId: Long!) {
  shopperNotifications(customerId: $customerId) {
      id,
      customerId,
      message,
      context,
      iconUrl,
      status,
      createdAt,
      updatedAt,
      appData,
      amount,
      displayType,
      displayId,
      itemId,
      itemType,
      messageKey,
      messageReplacements,
      notificationTime
  }
}